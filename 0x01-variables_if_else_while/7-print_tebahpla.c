#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * 
 *
 * 8th alx c program
 */
int main(void)
{
	char low;
	for (low = 'z'; low >= 'a'; low--)
		putchar(low);
	putchar('\n');
	return (0);
}
